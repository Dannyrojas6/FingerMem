# 日常 AI 辅助开发工作流

> 目标：让新 Agent / 新会话能够**快速理解整个项目**，实现高效热启动。

---

## 1. 结束一天工作前（EOD）

### 1.1 普通日子（轻量模式）—— 日常推荐

**触发时机**：你准备结束工作时，让当前 Agent 执行以下指令：

```text
请执行轻量 EOD 流程：
1. 读取 .understand-anything/knowledge-graph.json 和 fingerprints.json，建立当前项目理解。
2. 观察今天修改过的文件（主要通过文件修改时间）。
3. 更新/撰写项目根目录的 CURRENT_STATE.md，内容包含：
   - 当前正在做的任务/目标（可写得详细一些）
   - 项目当前整体状态（架构、热点模块、已知问题等）
   - 今天对项目的影响或变化（如果有）
   - 目前存在的开放问题或需要注意的地方
   - 下一步建议关注的方向（可选）
4. 如果发现明显的结构变化，请更新 fingerprints.json。
5. 不要主动触发完整的 /understand，除非我特别要求。
```

执行完成后，把 `CURRENT_STATE.md` 保留在根目录即可。

### 1.2 重大变化日（完整模式）

出现以下情况时，手动触发完整分析：

- 架构大调整
- 新增或重构核心模块
- 长时间未做完整分析
- 想刷新整个知识图谱

**触发指令示例**：

```text
今天项目有较大变化，请执行完整的 /understand 分析。
分析完成后，把 .understand-anything/ 下的关键文件复制到 .understand-snapshots/2026-06-XX/ 目录下归档。
同时更新 CURRENT_STATE.md。
```

归档目录示例：`.understand-snapshots/2026-06-15/`

---

## 2. 第二天开始工作时（SOD）

新 Agent 或新会话启动时的**推荐操作顺序**：

### 推荐阅读顺序

1. **首先阅读** 项目根目录的 `CURRENT_STATE.md`
   - 快速了解“现在在干什么”和“项目当前是什么状态”。

2. **然后阅读** `.understand-anything/knowledge-graph.json`
   - 获取完整的结构化项目理解（分层、组件关系、导览、关键逻辑等）。

3. 必要时再深入阅读具体源代码文件。

### 推荐的初始提示词（直接复制使用）

```text
请先阅读项目根目录下的 CURRENT_STATE.md，了解当前任务和项目状态。

然后阅读 .understand-anything/knowledge-graph.json，建立对整个项目的结构化理解。

理解完成后，告诉我你对项目的整体认知，并询问我今天想继续做什么。
```

---

## 3. 关键文件说明

| 文件 / 目录                        | 作用 | 日常维护方式 |
|------------------------------------|------|--------------|
| `CURRENT_STATE.md`                 | 当前任务 + 项目状态快照（新 session 必读） | 每天 EOD 更新 |
| `.understand-anything/knowledge-graph.json` | 完整结构化知识图谱 | 尽量保持较新 |
| `.understand-anything/fingerprints.json` | 结构指纹（用于未来增量分析） | 按需更新 |
| `.understand-snapshots/YYYY-MM-DD/` | 重大变化时的完整归档 | 仅在需要时手动创建 |

---

## 4. 注意事项

- **日常以轻量为主**：除非架构有明显变化，否则不要每天都跑完整的 `/understand`，成本不划算。
- **Git 初始化后**：本地 Git 会让 Agent 更准确地判断当天改动，增量分析能力也会提升。
- **远程仓库**：目前按干净原则设计，AI 辅助产生的文件可通过 `.gitignore` 控制是否提交（后续可根据实际需求调整）。
- 这个流程的核心目标是**降低新会话的理解成本**，而不是记录详细工作日志。

---

**版本**：v1.0  
**最后更新**：2026-05-30  
**设计依据**：方案一（简洁实用）